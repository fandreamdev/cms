{
  "$schema": "http://json-schema.org/schema",
  "$id": "CmsTreeSchema",
  "title": "CMS Tree CRUD Options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Entity name, for example role."
    },
    "desc": {
      "type": "string",
      "description": "Localized entity description.",
      "default": ""
    }
  },
  "required": ["name"]
}
